# Copilot Instructions

## Project Guidelines
- For ExpTree/ExpList, out-of-band OS updates may be lazy/deferred, but file operations initiated within the control must update control state immediately (directly or via non-deferred refresh).